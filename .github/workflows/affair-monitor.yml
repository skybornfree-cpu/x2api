name: Affair Monitor

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PYTHONUNBUFFERED: "1"

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

on:
  schedule:
    - cron: '8,48 * * * *'
  workflow_dispatch:
    inputs:
      base_url:
        description: 'Affair/911bl category entry'
        required: false
        default: 'https://affair.zhkrsawaw.cc/category/jrgb/'
      max_pages:
        description: 'Max pages per run'
        required: false
        default: '2'

jobs:
  monitor-affair:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collector/requirements.txt

      - name: Install dependencies
        working-directory: collector
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Affair monitor
        working-directory: collector
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
          REDIS_NAMESPACE: x2api
          BASE_URL: ${{ inputs.base_url || vars.AFFAIR_BASE_URL || 'https://affair.zhkrsawaw.cc/category/jrgb/' }}
          MAX_PAGES: ${{ inputs.max_pages || vars.AFFAIR_MONITOR_MAX_PAGES || '2' }}
          RETENTION_HOURS: ${{ vars.AFFAIR_RETENTION_HOURS || '84' }}
        run: |
          python twitter_monitor.py monitor-affair \
            --base-url "${BASE_URL}" \
            --max-pages "${MAX_PAGES}" \
            --retention-hours "${RETENTION_HOURS}"
