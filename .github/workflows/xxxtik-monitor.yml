name: xxxtik Monitor

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PYTHONUNBUFFERED: "1"

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

on:
  schedule:
    - cron: '35 0-23/2 * * *'
    - cron: '15,55 1-23/2 * * *'
  workflow_dispatch:
    inputs:
      base_url:
        description: 'xxxtik site entry'
        required: false
        default: 'https://xxxtik.com'
      max_pages:
        description: 'Max pages per run'
        required: false
        default: '1'

jobs:
  monitor-xxxtik:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collector/requirements.txt

      - name: Install dependencies
        working-directory: collector
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run xxxtik monitor
        working-directory: collector
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
          REDIS_NAMESPACE: x2api
          BASE_URL: ${{ inputs.base_url || vars.XXXTIK_BASE_URL || 'https://xxxtik.com' }}
          MAX_PAGES: ${{ inputs.max_pages || vars.XXXTIK_MONITOR_MAX_PAGES || '1' }}
          RETENTION_HOURS: ${{ vars.XXXTIK_RETENTION_HOURS || '168' }}
        run: |
          python twitter_monitor.py monitor-xxxtik \
            --base-url "${BASE_URL}" \
            --max-pages "${MAX_PAGES}" \
            --retention-hours "${RETENTION_HOURS}"
