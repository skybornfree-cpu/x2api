name: RouVideo Monitor

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: x2ding-rou-monitor
  cancel-in-progress: false

on:
  schedule:
    - cron: '5,45 0-23/2 * * *'
    - cron: '25 1-23/2 * * *'
  workflow_dispatch:
    inputs:
      base_url:
        description: 'RouVideo 站点入口'
        required: false
        default: 'https://rou.video/v'
      max_pages:
        description: '单次最多分页数'
        required: false
        default: '2'

jobs:
  monitor-rou:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collector/requirements.txt

      - name: Install dependencies
        working-directory: collector
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run RouVideo monitor
        working-directory: collector
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          BASE_URL: ${{ inputs.base_url || vars.ROU_BASE_URL || 'https://rou.video/v' }}
          MAX_PAGES: ${{ inputs.max_pages || vars.ROU_MONITOR_MAX_PAGES || '2' }}
        run: |
          python twitter_monitor.py monitor-rou \
            --base-url "${BASE_URL}" \
            --max-pages "${MAX_PAGES}" \
            --retention-hours 84
