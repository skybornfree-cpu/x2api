name: 705HS Monitor

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PYTHONUNBUFFERED: "1"

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

on:
  schedule:
    - cron: '27 0-23/2 * * *'
    - cron: '7,47 1-23/2 * * *'
  workflow_dispatch:
    inputs:
      base_url:
        description: '705hs site entry'
        required: false
        default: 'https://705hs.com/Html/60/index-1.html'
      max_pages:
        description: 'Max pages per run'
        required: false
        default: '1'

jobs:
  monitor-705hs:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collector/requirements.txt

      - name: Install dependencies
        working-directory: collector
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run 705hs monitor
        working-directory: collector
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
          REDIS_NAMESPACE: x2api
          BASE_URL: ${{ inputs.base_url || vars.HS705_BASE_URL || 'https://705hs.com/Html/60/index-1.html' }}
          MAX_PAGES: ${{ inputs.max_pages || vars.HS705_MONITOR_MAX_PAGES || '1' }}
        run: |
          python twitter_monitor.py monitor-705hs \
            --base-url "${BASE_URL}" \
            --max-pages "${MAX_PAGES}" \
            --retention-hours 84
