name: Douyin Video Monitor

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  PYTHONUNBUFFERED: "1"

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

on:
  schedule:
    - cron: '0 0-23/2 * * *'
    - cron: '40 0-22/2 * * *'
    - cron: '20 1-23/2 * * *'
  workflow_dispatch:
    inputs:
      base_url:
        description: '抖阴站点入口'
        required: false
        default: 'https://xygrfrfb3g.b2h7y8w.com'
      max_pages:
        description: '单次最多分页数'
        required: false
        default: '2'

jobs:
  monitor-douyin:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collector/requirements.txt

      - name: Install dependencies
        working-directory: collector
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Douyin monitor
        working-directory: collector
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
          REDIS_NAMESPACE: x2api
          BASE_URL: ${{ inputs.base_url || vars.DOUYIN_BASE_URL || 'https://xygrfrfb3g.b2h7y8w.com' }}
          MAX_PAGES: ${{ inputs.max_pages || vars.DOUYIN_MONITOR_MAX_PAGES || '2' }}
        run: |
          python twitter_monitor.py monitor-douyin \
            --base-url "${BASE_URL}" \
            --max-pages "${MAX_PAGES}" \
            --retention-hours 84
